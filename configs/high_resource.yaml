# High Resource Language Configuration
# For languages with >30k examples: nr, tn, ts, xh, zu

training:
  batch_size: 16
  gradient_accumulation_steps: 2
  learning_rate: 5e-5
  max_steps: 5000
  warmup_steps: 500
  eval_steps: 500
  save_steps: 500

